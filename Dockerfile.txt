FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

# Fly.io imposta PORT automaticamente; main.py la legge da os.environ.
# Il valore qui è solo un fallback/documentazione.
ENV PORT=8765
EXPOSE 8765

CMD ["python3", "main.py"]
